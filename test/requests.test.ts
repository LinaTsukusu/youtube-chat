import { fetchLivePage, fetchChat } from "../src/requests"
import axios from "axios"

jest.mock("axios")

describe("requests", () => {
  describe("fetchChat", () => {
    test("Request", () => {

    })
  })

  describe("fetchLivePage", () => {
    test("ChannelID request", () => {

    })

    test("LiveID request", () => {

    })
  })
})
